<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyPuzzle" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DailyPuzzle" parent="Base.Theme.DailyPuzzle" />

    <style name="SyllableButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:fontFamily">@font/tajawal</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">0dp</item>
        <item name="android:textColor">#30475E</item>
        <item name="android:background">@drawable/syllable_button_bg</item>
    </style>









</resources>